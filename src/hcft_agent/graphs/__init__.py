# Hand-written LangGraph graphs live here (M0 onward).
# Convention: each module exposes a build_*_graph(...) factory returning a compiled graph,
# so checkpointers/models are injected and a future service layer can mount them unchanged.
