services:
  mongo:
    image: mongo:7
    container_name: hcft-mongo
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - hcft_mongo_data:/data/db
    restart: unless-stopped

volumes:
  hcft_mongo_data:
